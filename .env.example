# PostgreSQL Configuration
# For Render.com, this will be automatically provided
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Server Configuration
PORT=3001
NODE_ENV=development

# Optional: Set to 'true' to use local JSON file instead of PostgreSQL
USE_LOCAL_DB=false

# Admin Mode
ENABLE_ADMIN=false
ADMIN_TOKEN=changeme
